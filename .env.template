# House KB — environment config
# Copy to .env and fill in values before running.
# Generate secret: python3 -c "import secrets; print(secrets.token_hex(32))"

HOUSE_SECRET=replace-with-generated-secret
HOUSE_DB=/path/to/house/data/house.db
HOUSE_UPLOADS=/path/to/house/uploads
